<p id="notice"><%= notice %></p>

<h1>Listing beers</h1>

<table id="beertable" class="table table-hover">
  <tr>
    <th> <a href="#" id="name">Name</a> </th>
    <th> <a href="#" id="style">Style</a> </th>
    <th> <a href="#" id="brewery">Brewery</a> </th>
  </tr>
</table>

<%= link_to 'New Beer', new_beer_path, class:'btn btn-primary btn-block' if current_user %>
<%= link_to 'List of breweries', breweries_path, class:'btn btn-default btn-block' %>
