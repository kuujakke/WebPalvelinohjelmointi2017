<h2>List of ratings</h2>

<ul>
  <% @ratings.each do |rating| %>
      <li> <%= rating %> <%= link_to 'Destroy', rating, method: :delete, data: { confirm: "Really delete rating?" } %> </li>
  <% end %>
</ul>
<%= link_to 'New', new_rating_path %>
