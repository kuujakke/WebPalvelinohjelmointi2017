<p id="notice"><%= notice %></p>
<h1>Listing styles</h1>
<% unless @styles.empty? %>
    <ul>
      <% @styles.each do |s| %>
          <li>
            <%= link_to(s.name, style_path(s)) %>
          </li>
      <% end %>
    </ul>
<% end %>